# Terraform-Variablen für das Immobilien-Scraper-Projekt

# AWS-Region
aws_region = "eu-central-1"

# Projektname
project_name = "vibtellect-immo-scraper"

# Zeitplan für Lambda-Ausführung
schedule_expression_1 = "cron(0 8 * * ? *)"  # Jeden Tag um 8:00 UTC
schedule_expression_2 = "cron(0 20 * * ? *)" # Jeden Tag um 20:00 UTC

# Benachrichtigungen
notification_email = ""  # Füge hier deine E-Mail-Adresse ein

# Telegram-Konfiguration
# Diese Werte werden aus der config.json geladen
telegram_bot_token = "YOUR_TELEGRAM_BOT_TOKEN"
telegram_chat_id = "YOUR_TELEGRAM_CHAT_ID"
